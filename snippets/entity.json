{
  "@Abstract entity Annotation": {
    "prefix": ["abs", "@Abstract"],
    "description": "Marks an entity as \"abstract\"",
    "body": [
      "@Abstract"
    ]
  },
  "@AggregateRoot entity Annotation": {
    "prefix": ["agg", "@AggregateRoot"],
    "description": "Marks an entity as \"aggregate root\" (useful to define \"DDD aggregates\" )",
    "body": [
      "@AggregateRoot"
    ]
  },
  "@Context() entity Annotation": {
    "prefix": ["cont", "@Context()"],
    "description": "Defines the context to which the entity belongs",
    "body": [
      "@Context(\"$1\")"
    ]
  },
  "@DbCatalog() entity Annotation": {
    "prefix": ["dbca", "@DbCatalog()"],
    "description": "Defines the database catalog to which the entity table belongs",
    "body": [
      "@DbCatalog(\"$1\")"
    ]
  },
  "@DbComment() attribute & entity Annotation": {
    "prefix": ["dbco","@DbComment()"],
    "description": "The comment in the database (typically a column comment or table comment in a relational database)",
    "body": [
      "@DbComment(\"$1\")"
    ]
  },
  "@DbSchema() entity Annotation": {
    "prefix": ["dbsc", "@DbSchema()"],
    "description": "Defines the database schema to which the entity table belongs",
    "body": [
      "@DbSchema(\"$1\")"
    ]
  },
  "@DbTable() entity Annotation": {
    "prefix": ["dbt", "@DbTable()"],
    "description": "Defines the database table associated with the entity",
    "body": [
      "@DbTable(\"$1\")"
    ]
  },
  "@DbTablespace() entity Annotation": {
    "prefix": ["dbts", "@DbTablespace()"],
    "description": "Defines the database tablespace where the entity table is located",
    "body": [
      "@DbTablespace(\"$1\")"
    ]
  },
  "@DbView entity Annotation": {
    "prefix": ["dbv", "@DbView"],
    "description": "Marks the entity as a \"view\" in the database",
    "body": [
      "@DbView"
    ]
  },
  "@Domain() entity Annotation": {
    "prefix": ["dom", "@Domain()"],
    "description": "Defines the \"domain\" to which the entity belongs",
    "body": [
      "@Domain(\"$1\")"
    ]
  },
  "@Extends() entity Annotation": {
    "prefix": ["ext", "@Extends()"],
    "description": "Defines the \"superclass\" of an entity (inheritance)",
    "body": [
      "@Extends(\"$1\")"
    ]
  },
  "@InMemoryRepository entity Annotation": {
    "prefix": ["inm", "@InMemoryRepository"],
    "description": "Marks an entity as fully stored in memory. All occurrences can be accessed directly in memory (can be useful for static datasets).",
    "body": [
      "@InMemoryRepository"
    ]
  },
  "@Package() entity Annotation": {
    "prefix": ["pack", "@Package()"],
    "description": "Defines the \"package\" to which the entity belongs",
    "body": [
      "@Package(\"$1\")"
    ]
  },
  "@ReadOnly entity Annotation": {
    "prefix": ["rea", "@ReadOnly"],
    "description": "Marks the entity as \"readonly\"",
    "body": [
      "@ReadOnly"
    ]
  }
}